import { EventModalTemplates } from './event-modal-templates';

describe('EventModalTemplates', () => {
  it('should create an instance', () => {
    expect(new EventModalTemplates()).toBeTruthy();
  });
});
