export interface LanguageInfo {
    name: string;
    displayName: string;
    icon: string;
    isDefault: boolean;
    isDisabled: boolean;
    isRightToLeft: boolean;
}

export interface SourceInfo {
    name: string;
    type: string;
}
